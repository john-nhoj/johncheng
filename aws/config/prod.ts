import { ConfigProps } from '../typings/config';

const prodConfig: ConfigProps = {
  serviceName: 'johncheng',
  environment: 'prod',
  certificateArn: 'TBA',
  hostedZoneId: 'TBA',
};

export { prodConfig };
